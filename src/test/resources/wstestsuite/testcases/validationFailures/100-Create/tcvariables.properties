# This file is optional/empty or contains any additional variable that needs to be defined in addition to below default variables and test suite variables.
#TESTCASE/HOSTNAME=InetAddress.getLocalHost().getHostName()
#TESTCASE/HOSTIP=InetAddress.getLocalHost().getHostAddress()
#TESTCASE/STARTTIME=""+System.currentTimeMillis()
#TESTCASE/UUID=UUID.randomUUID().toString()
#TESTCASE/RAND5=Math.round(Math.random()*99999)
#TESTCASE/RAND10=Math.round(Math.random()*9999999999)
#
# The variable values could be java(beanshell) expression with default variables
# The variable keys will automatically get prefixed with TESTCASE/
#
DOUBLE_UUID=UUID.randomUUID().toString()\ +\ UUID.randomUUID().toString()
